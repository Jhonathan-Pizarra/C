
//#include <stdio.h>

//int main(){

    //int final = 10;
    //int inicio = 0;

    /*
    while (inicio < final){
        printf("%i ", inicio);
        inicio ++; //Python no tiene estos operadores
    }

    */
    //printf("\n");
    //El bucle do-while se ejecuta al menos una vez El ciclo do- while siempre se ejecuta al menos una vez,
    // incluso si la expresión se evalúa como falsa.
    // (RECUERDA que en el bucle anterior el valor de inicio quedó en 10, por lo tanto
    //si quieres usar otro bucle, considera que el valor de inicio ya no será 0 sino 10

    /*
    do{

        printf("%i ", final);
        final --;

    }while(final > inicio); //no olives el ";"
     */

 //   printf("\n");
/*
    //BUCLE FOR
    int i=0;
    int max = 10;
    for (int i; i < max; i++) {
        printf("%i ", i);
    }

    //BUCLE FOR con centinela
    int ward = 0;
    int max = 10;
    for (int i = 0; i < max; i++) {
        ward = i;
        printf("%i ", i);
    }
    printf("%i ", ward);

    printf("\n");
    //FOR ANIDADO (TABLA DE MULTIPLICAR
    int X, Y;
    int tabla = 10;
    int end = 12;

    for (X = 1; X <= tabla; X ++) {
        for (Y = 0; Y <= end; Y ++) {
            printf ("%i x %i = %i \n", X, Y, (X * Y));
        }
        printf("\n"); // línea en blanco entre tablas
    }



    return 0;
     */
//}

